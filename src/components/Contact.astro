<div class="popup-container left-align">
  <div class="popup-card">
    <div class="popup-title">Contáctanos</div>
    <form class="popup-form" id="loginForm">
      <input
        class="popup-input"
        id="emailInput"
        name="email"
        placeholder="Email"
        required
      />
      <input
        class="popup-input"
        id="passwordInput"
        name="password"
        placeholder="Contraseña"
        required
      />
      <textarea
        class="popup-input"
        name="mensaje"
        placeholder="Mensaje..."
      ></textarea>
      <button class="popup-button" type="submit">Enviar</button>
    </form>
  </div>
</div>


<style>
  @import url('https://fonts.googleapis.com/css2?family=Roboto+Serif:opsz,wght@8..144,800&display=swap');
  /* === ESTILOS BASE (MOBILE FIRST) === */
  .popup-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    background: rgba(0, 0, 0, 0.6);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    box-sizing: border-box;
  }

  .popup-container.left-align {
    justify-content: flex-start;
  }

  .popup-card {
    background-color: oklch(0.9456 0.013 185.09 / 68.58%);
    padding: 40px 20px;
    border-radius: 20px;
    border: 2px solid #333;
    box-shadow: 6px 6px #000000;
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    max-width: 400px; /* Cambiado para reducir el ancho horizontal */
    box-sizing: border-box;
  }

  .popup-title {
    font-family: 'Roboto Serif', serif;
    font-size: 20px;
    font-weight: bold;
    color: #000;
    text-align: center;
    margin-bottom: 25px;
    padding-bottom: 10px;
  }

  .popup-form {
    display: flex;
    flex-direction: column;
    gap: 15px;
    width: 100%;
  }

  .popup-input {
    width: 90%;
    margin: 0 auto;
    padding: 12px;
    font-size: 17px;
    border-radius: 6px;
    border: 2px solid #333;
    box-shadow: 3px 3px #333;
    resize: none;
    font-family: "Courier New", Courier, monospace;
    box-sizing: border-box;
  }

  .popup-button {
    width: 100%;
    padding: 12px;
    background-color: #fff;
    font-weight: bold;
    border-radius: 10px;
    border: 2px solid #333;
    box-shadow: 3px 3px #333;
    cursor: pointer;
    transition: transform 0.1s;
    font-family: 'Roboto Serif', serif;
    margin-top: 20px;
    font-size: 18px;
  }

  .popup-button:active {
    transform: translate(2px, 2px);
    box-shadow: 0 0 #333;
  }

  /* === TABLETS (≥768px) === */
  @media screen and (min-width: 768px) {
    .popup-card {
      padding: 50px 30px;
      border-radius: 25px;
      max-width: 450px;
    }

    .popup-title {
      font-size: 22px;
    }

    .popup-button {
      font-size: 17px;
    }

    .popup-input {
      font-size: 15px;
    }
  }

  /* === DESKTOPS (≥992px) === */
  @media screen and (min-width: 992px) {
    .popup-container {
      padding: 60px;
    }

    .popup-card {
      padding: 70px 40px;
      max-width: 500px;
      border-radius: 30px;
    }

    .popup-title {
      font-size: 24px;
    }
  }

  /* === LARGE DESKTOPS (≥1200px) === */
  @media screen and (min-width: 1200px) {
    .popup-card {
      max-width: 550px;
    }
  }
</style>
